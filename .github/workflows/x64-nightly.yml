name: X64 Nightly

on:
  schedule:
    - cron: '0 16 * * 6,0'
  workflow_dispatch:

concurrency:
  group: x64-nightly-${{ github.ref }}
  cancel-in-progress: true

jobs:
  nightly:
    uses: ./.github/workflows/tier-reusable.yml
    with:
      job_name: x64 / nightly
      runner: ubuntu-24.04
      tier: nightly
      timeout_minutes: 60
